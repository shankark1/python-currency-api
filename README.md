# Python Currency Converter API

This is my currency converter API project using Flask.

## Features
- Convert one currency to another
- Uses external exchange rate API
- Returns converted amount in JSON format

## How to Run

```bash
pip install flask requests
python currency.py
